extends ../base
include ../mixins/video

block content
    form(method="GET").search__form
        input(name="keyword", placeholder="Search by title", type="text")
        button 
            i.fas.fa-search
    div.video-grid
        each content in videos 
            +video(content)
        else 
            span.empty__message No videos found