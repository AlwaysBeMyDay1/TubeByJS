extends ../base
include ../mixins/video 

block content
    div.profile__data
        img(src="/"+user.avatarUrl).avatar
        h3=user.name
        br
        a(href="/users/edit") Edit Profile 
    div.video-grid
        each content in user.videos
            +video(content)
        else
            li Did not upload video yet 😥